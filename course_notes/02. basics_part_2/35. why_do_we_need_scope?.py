# ==== why do we need scope? ====

# if every variable was in the global scope it would use more power and memory

# by using functions we save memory because after the function runs what is inside of it is removed from memory